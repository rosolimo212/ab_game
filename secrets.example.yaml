# Скопируйте в secrets.yaml и заполните значения.
# secrets.yaml НЕ должен попадать в git (.gitignore).
#
# При app.logging_enabled=false файл secrets.yaml можно не создавать.

logging:
  password: YOUR_PASSWORD_HERE

testing:
  password: YOUR_TESTER_PASSWORD_HERE

# Раскомментируйте, когда появится Telegram:
# telegram:
#   token: YOUR_TOKEN_HERE
